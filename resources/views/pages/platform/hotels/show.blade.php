@extends('templates.index')
@section('content')<div class="app-main__inner"><div class="app-page-title"><div class="page-title-wrapper">@include('templates.parts.breadcrumb', ['title'=>$hotel->name,'icon'=>$icon,'breadcrumbs'=>[['href'=>route('platform.hotels.index'),'label'=>'Hotel'],['href'=>'#','label'=>'Detail']]])<div class="page-title-actions"><a href="{{ route('platform.hotel-admins.create',['hotel_id'=>$hotel->id]) }}" class="btn btn-success">Tambah Admin</a> <a href="{{ route('platform.hotels.edit',$hotel) }}" class="btn btn-primary">Edit Hotel</a></div></div></div>
<div class="row"><div class="col-md-4"><div class="card mb-3"><div class="card-body"><h5>{{ $hotel->code }}</h5><p class="mb-1">Status: {{ $hotel->is_active ? 'Aktif' : 'Nonaktif' }}</p><p class="mb-1">Kunjungan: {{ $hotel->visits_count }}</p><p class="mb-0">Storage: <code>{{ $hotel->configuration?->media_root }}</code></p></div></div></div>
<div class="col-md-8"><div class="card"><div class="card-header">User di Hotel Ini</div><div class="card-body table-responsive p-0"><table class="table table-striped mb-0"><thead><tr><th>Nama</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>@forelse($hotel->users as $user)<tr><td>{{ $user->profile?->name ?? $user->username }}</td><td>{{ $user->email }}</td><td>{{ $user->role?->name }}</td><td>{{ $user->is_active ? 'Aktif' : 'Nonaktif' }}</td></tr>@empty<tr><td colspan="4" class="text-center">Belum ada user.</td></tr>@endforelse</tbody></table></div></div></div></div></div>@endsection
